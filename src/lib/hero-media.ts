/**
 * Home hero media. The still pair always ships; the ambient loop (a 5 s
 * image-to-video drift of the same plate) is behind this flag, OFF until Ethan
 * chooses still vs loop. When ON it plays on desktop only — never on phones,
 * never under prefers-reduced-motion — with the still underneath as poster.
 */
export const HERO_AMBIENT_LOOP = false;
export const HERO_LOOP_MP4 = '/media/hero-loop.mp4';
export const HERO_LOOP_WEBM = '/media/hero-loop.webm';
